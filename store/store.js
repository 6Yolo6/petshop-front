import {
	createStore
} from 'vuex'

const store = createStore({
	state: {
		// 定义状态属性
	},
	mutations: {
		// 定义状态变更方法
	},
	actions: {
		// 定义异步操作方法
	},
	getters: {
		// 定义计算属性
	}
})

export default store